# Codex `sart` Command Alias

User protocol:
- if you type `sart` by mistake, this alias will run `start`.

Adapter entry:
- `bash .codex/commands/sart.sh`

What happens:
1. Prints alias notice (`sart -> start`).
2. Delegates to `.codex/commands/start.sh`.
